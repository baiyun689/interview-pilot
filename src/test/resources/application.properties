app.jwt.hmac-secret=test-hmac-secret-that-is-at-least-32-chars-long-for-testing
# The voice cleanup sweeper never runs on its own in tests: the schedule is pinned far in the
# future and cleanup ITs invoke the service directly (Task 11).
app.voice.cleanup-interval=PT24H
app.voice.cleanup-initial-delay=PT24H
