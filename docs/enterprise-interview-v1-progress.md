# 企业招聘 V1 实施进度

目标与范围见 [实施方案](enterprise-interview-v1-plan.md)。用户已授权按完整方案实现，并要求最后创建 1 个企业账号、若干合成求职账号，实际测试效果。不得将阶段完成称为 V1 已完成。

当前：阶段 A 基础链路已实现；阶段 B 已实现岗位证据分析、面试方案版本和企业知识库，独立题库仍待补齐。阶段 C 已接通站内通知、SMTP 邮件及版本化提醒。阶段 D 已接通邀请执行、内部报告、人工评审修订、独立公开反馈及文本/语音作答版本约束。完整 V1 仍有独立题库、超出窗口的改期审批及阶段 E 配额/运维/压测等验收工作；不能把本轮四项完成称为全部 V1 已完成。未提交、未推送。

## 已实现

- V27：企业/成员邀请/岗位授权、岗位与发布版本、简历投递快照、投递/撤回/显式重新投递、事件和审计。
- V28：平台运维授权表、企业停用状态和受保护平台页面。
- 新 recruitment 模块：JPA 持久化、显式企业/岗位授权、READ_COMMITTED 写事务与组织/岗位行锁。
- 企业页面、公开岗位/投递入口、候选人投递与材料快照、团队管理、岗位授权。
- 新增实际 MySQL 集成测试、前端交互测试。
- V29：岗位证据分析异步任务（执行租约、代次隔离、有限重试）、面试方案与不可变发布版本。
- V30：企业知识库显式归属、关键词/向量检索企业过滤、方案引用保护与异步文档删除。
- 前端导航分组、企业工作区选中态、独立企业设置、岗位内展开材料/方案、手机横向导航；切换岗位重置投递状态。
- V31：面试批次、成员与邀请；企业作用域幂等键、同投递同轮次有效邀请唯一约束、逐人准备/确认/部分下发、取消/拒绝/过期和计划时间版本。
- 公共题与编号由服务端固定，AI 只补齐定制题；使用冻结企业知识范围检索并把真实片段提供给模型，校验简历/知识来源；审批只接受服务端保留的知识快照。预览可修改定制题、评分标准及引用。
- 批次保留成员原始岗位版本；批次计数使用 SQL 聚合；知识引用在硬截止加保留期后过期（默认 90 天，可配置）。

## 验证

- 初次后端编译、前端构建通过。
- RecruitmentFoundationIT：10 项通过，覆盖冻结材料、越权、并发重复投递、显式重投、旧邀请、停用、版本冲突及并发最后管理员保护。
- HiringAssessmentIT：5 项通过；证据校验、企业关键词隔离与向量过滤等定向回归通过。
- 前端构建与全量 17 文件、150 项测试通过（导航及布局调整后）。
- 已在本地真实服务验证：企业账号登录、岗位发布、3 个求职账号上传合成简历并投递、企业查看材料、候选人查看自己的投递；1 次真实模型岗位证据分析完成（1 次执行，无重试）。这不代表模型质量评估已完成。
- 浏览器检查桌面工作台、公开岗位和候选人页面；390px 宽度下卡片未横向溢出，岗位内展开可用。修复登录回跳竞争，实际求职账号登录后正确返回投递页。
- 本地应用已应用 V27—V31；后续数据库变更必须追加迁移，不能改写这些迁移。
- 代码审查发现 MySQL RR 旧快照问题，已改为写事务 READ_COMMITTED，实际并发测试和审查复核通过。
- 方案审查提出岗位转换/企业更名/平台导航缺项，已补代码，后续复验。
- 后端最终全量回归、完整招聘流程演示、后续各阶段验收尚未完成。
- 本轮 RecruitmentFoundationIT 10 项、HiringAssessmentIT 5 项、HiringCampaignIT 7 项通过；HiringPreparationEngineTest 验证冻结企业知识送入模型、来源校验及服务端公共题组装。
- 前端新增邀请确认/拒绝测试；18 文件 152 项全量通过。首次并行全量中原语音播放器测试出现一次时序失败，单独复跑及再次全量均通过，未修改播放器代码。
- 本轮双轴审查发现的 6 项均已修复并只读复核：检索未使用、历史岗位版本、引用预览、引用期限、统计查询、null 题卡校验。
- 真实批次联调中发现模型输出字段不稳定，已补充明确 JSON 结构，并改为服务端组装公共题。批次 1 已有 2 人题目完成；浏览器确认陈同学后只下发该成员，显示已下发 1 人/未下发 2 人；陈同学已在候选人页面安排 2026-09-10 10:00（Asia/Shanghai）。林同学的任务因开发重启正在等待租约恢复，后续须核对最终状态。
- 后端全量回归正在运行（.codex-campaign-tests.log）；发现任务策略扫描遗漏 HIRING_WORK，已把策略移入 async.policy 并更新扫描断言，需在全量结束后验证修复。

## 本地演示环境

- 前端 http://127.0.0.1:5173 ，后端 http://localhost:8080 。
- 企业：hr.demo@example.test；求职者：candidate.lin@example.test、candidate.chen@example.test、candidate.zhou@example.test。
- 凭据保存在被 Git 忽略的 `.codex-local/demo-accounts.json`，仅供本地演示。
- 企业为「星程科技（演示）」，2 个公开岗位，实习岗位已有 3 次投递。
- 运行中：本地 Vite、bootRun；Compose mysql/redis/rabbitmq；独立 Compose run 容器 interview-pilot-qdrant-local 仅映射 127.0.0.1:6334。未部署外网、未发送邮件。
- 修正现有日志格式 `%c{1.}` 为 `%c{1}`，解决启动时 Logback 格式解析错误。

## 下一步

### 2026-09-09 人工评审、反馈、通知与并发保护

- V33 已在本地应用：评审指派、草稿、不可变提交修订、独立发布版本、通知台账及 SMTP 尝试记录。后续不能改写 V33。
- 企业新增“面试与评审”“通知记录”；候选人新增通知中心、邀请中的公开反馈。内部报告、逐题 AI 结果、冻结评分依据及知识原文仅企业授权人员可见。
- 评审草稿允许暂存不完整内容；提交校验维度与回答证据。面试官需逐面试显式指派，只有管理员/授权招聘人员可以发布；发布使用独立版本防止并发覆盖。下一轮沿用既有批次入口；结束流程后不再接受新邀请。
- 通知与业务同事务创建，SMTP 在独立线程发送；唯一事件键、数据库租约、多实例领取、有限重试、UNKNOWN 不自动重发。旧日程提醒执行前检查邀请状态与调度版本，过时提醒跳过。
- 招聘作答增加题目/会话版本并纳入幂等指纹；实时语音绑定实际下发问题的版本，丢弃重复提交及旧识别回调代次。
- 本地 SMTP 测试接收器仅监听回环地址、仅接受 `.test` 邮箱，不向外转发。陈同学真实模型报告已用于浏览器评审演示：企业提交人工意见、预览并发布公开反馈，候选人可见，内部报告仍为 403；反馈邮件首次尝试被本地接收器接受。
- 代码双轴审查及语音增量复核提出草稿校验、null 维度、SMTP 调度隔离、逐题依据展示、识别回调入队竞争五项，均已修复并只读复核。
- 配置与操作说明：[评审和通知指南](hiring-review-notification-guide.md)。最终冻结版本回归通过：后端 149 个测试套件、806 项（796 通过、10 跳过）、0 失败/错误，耗时 14 分 20 秒（`.codex-review-clean-full-tests.log`）；前端 19 个文件、156 项全部通过，生产构建通过。期间一次后端重启与测试并发编译导致的类加载失败已作废，本结果来自无并发构建的完整重跑。

### 2026-09-09 恢复后的本轮增量（仍未完成 V1）

- 已实现本人鉴权的 ICS 下载，稳定 UID/调度 SEQUENCE、UTC 时间、取消状态、中文 UTF-8 折行及文本转义；候选人页面提示变更后重新导入。单元、权限集成和浏览器下载动作测试通过；常见日历客户端实际导入尚未验收。
- V32 已在本地应用：邀请唯一绑定核心面试会话、冻结执行顺序、服务端截止时间。新增企业邀请开始/恢复、按企业确认题卡执行、零追问、超时收尾与进行中撤回/终止。个人练习保留原规则。
- 企业面试从个人练习历史中分开，候选人报告接口返回 403；候选人作答页不显示内部报告，后续人工公开反馈仍待实现。
- 新增开始并发、窗口、权限、冻结题卡、截止时间、取消与超时保留已接收答案的真实 MySQL 测试；修复并发重放的微秒精度差异。
- 本轮双轴审查发现两项已修复且只读复核通过：截止后模型回写误将已接收答案标失败；招聘报告仍强制四阶段分数。对应定向回归通过。
- 前端全量 18 文件 153 项通过。旧 QuestionSpeechPlayer 测试曾在全量并行时提前发送 canplay，现等待 React 挂载副作用完成，未改变播放器产品行为。
- 已用陈同学浏览器实际完成 2 道企业题：公共缓存一致性题和简历校园预约系统定制题。会话 `2a9f55ab-2ab3-411b-81a6-7f517d6ffd2d`，两次回答持久化成功；候选人内部报告访问实际返回 403，ICS 接口实际返回 200。
- 真实模型报告失败已定位并修复：模型臆造知识状态，通用示例诱导输出四阶段分数。招聘报告现在由服务端从冻结证据填充知识状态，并按实际正式作答阶段生成输出结构，仍严格校验分数阶段和来源。新增 AiRecruitmentReportGeneratorTest。对原有回答重试后，报告任务与会话均实际 COMPLETED（2026-09-09 18:44 左右）。
- 批次 1 三名成员准备任务现在均 COMPLETED、均已确认下发。陈同学已完成作答及真实模型报告；林/周同学分别安排 2026-09-10 10:30 / 11:00（Asia/Shanghai），可用于后续浏览器演示。
- 新后端全量回归通过：790 项、0 失败、10 跳过（`.codex-execution-full-tests.log`，14 分 18 秒）；随后最后报告字段修复的 4 项定向回归通过（`.codex-report-final-tests.log`）。前端 153 项通过，构建通过。全量运行过程中最后报告诊断/生成器修复使用专项测试及原场景实际重试补充验证，未声称再次完整运行全部后端测试。
- 最后实际验证：报告分数阶段仅 FUNDAMENTALS；完成后内部报告仍返回 403，招聘会话不出现在个人练习历史。全量结果另存被忽略的 `.codex-local/execution-full-test-result.json`。
- 当前后端为最新 bootRun，前端 Vite 仍运行。本轮没有提交或推送。优先继续：作答版本约束（含多标签页/实时语音的轮次绑定）、人工评审/公开反馈、可靠通知/改期提醒、独立题库和运维验收。

1. 验证 V28 平台身份和停用；完善 HTTP 权限测试与迁移断言。
2. 阶段 B：异步岗位证据分析、企业题库/方案与版本、企业知识检索范围、方案驱动面试。
3. 阶段 C：批次逐人准备和人工确认、邀请、可靠通知、日程改期提醒及 ICS。
4. 阶段 D：邀请驱动执行、时间/取消/断线、证据报告、人工评审与下一轮。
5. 阶段 E：配额、运维任务、监控、RAG 实测、压测/恢复、完整演示账号与验收报告。

## 保留约束

- 起始 HEAD：28adb21ccae61c87dbf1705fdb9ccbe392606ada，main。
- 起始已有未提交的 V26 混合检索改动，必须保留。
- 不删除数据库，不改写历史迁移，不自动发送真实外部邮件。
- 仅合成简历与测试邮箱用于演示；测试模型桩的数据不能当成真实模型质量/性能结论。
